# Copy to .env and fill in. The backend runs WITHOUT a key (it falls back to a
# deterministic mock tagger), so a key is only needed for real Gemini calls.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
